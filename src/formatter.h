// Formats partition on disk. Creates and initializes `partition.io` on disk.
void format_partition();

// Formats directory file on disk. Creates and initializes `directory.dir` on disk.
void format_directory();
